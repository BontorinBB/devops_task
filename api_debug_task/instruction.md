You are given a Python API running inside a Docker container.

The application currently fails to start due to dependency or runtime issues.

Your task is to:
- Identify the root cause of the failure
- Fix the issue so the API runs successfully
- Ensure the API responds on port 5000

Constraints:
- Do not rewrite the entire project
- Only fix what is necessary